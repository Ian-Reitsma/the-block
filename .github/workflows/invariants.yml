name: invariants-status
on:
  pull_request:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run tests
        id: vars
        run: |
          cargo test --quiet && echo "status=passing" >> "$GITHUB_OUTPUT"
