apiVersion: v1
kind: Service
metadata:
  name: {{ include "the-block-gateway.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "the-block-gateway.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
