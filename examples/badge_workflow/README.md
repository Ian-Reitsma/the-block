# Badge Workflow Examples
Guidance aligns with the dependency-sovereignty pivot; runtime, transport, overlay, storage_engine, coding, crypto_suite, and codec wrappers are live with governance overrides enforced.

Simple scripts demonstrating service badge issuance, verification, and revocation
via the CLI. Run `./issue_and_revoke.sh` after starting a node with the RPC
server enabled.
