[package]
name = "p2p_overlay"
version = "0.1.0"
edition = "2021"

[dependencies]
libp2p = { version = "0.52", default-features = false, features = ["kad"] }
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"

[dev-dependencies]
tempfile = "3"

