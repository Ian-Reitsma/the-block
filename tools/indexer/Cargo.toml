[package]
name = "indexer"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
cli_core = { path = "../../crates/cli_core" }
foundation_serialization = { path = "../../crates/foundation_serialization" }
rusqlite = { version = "0.30", features=["bundled"] }
httpd = { path = "../../crates/httpd" }
runtime = { path = "../../crates/runtime" }
the_block = { path = "../../node", default-features = false }

[dev-dependencies]
sys = { path = "../../crates/sys" }

[build-dependencies]
dependency_guard = { path = "../../crates/dependency_guard" }
