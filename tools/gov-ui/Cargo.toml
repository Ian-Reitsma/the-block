[package]
name = "gov-ui"
version = "0.1.0"
edition = "2021"

[dependencies]
warp = "0.3"
serde = { version = "1.0", features = ["derive"] }
the_block = { path = "../../node" }
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
