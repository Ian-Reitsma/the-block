[package]
name = "release_notes"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
cli_core = { path = "../../crates/cli_core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
