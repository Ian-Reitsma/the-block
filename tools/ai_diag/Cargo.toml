[package]
name = "ai_diag"
version = "0.1.0"
edition = "2021"
authors = ["The Block"]
license = "Apache-2.0"

[dependencies]
diagnostics = { path = "../../crates/diagnostics" }

[dev-dependencies]
diagnostics = { path = "../../crates/diagnostics" }
