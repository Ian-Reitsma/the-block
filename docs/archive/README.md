# Documentation Archive

This directory holds historical or completed documentation snapshots that are no
longer maintained as live specs. Links across the repo now point here for
reference-only material.

If you need to update behavior, edit the canonical docs under `docs/` instead of
modifying archived files.
