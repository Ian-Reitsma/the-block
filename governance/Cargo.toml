[package]
name = "governance-tests"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dev-dependencies]
the_block = { path = "../node" }
tempfile = "3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
